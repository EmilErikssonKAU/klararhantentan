# About the project

This project is a betting-site for the examresults of classmates.

The backend is built with Node.js, Express.js and MongoDB, and uses JWTs for verifying Http requests.

The frontend is built with React.js.